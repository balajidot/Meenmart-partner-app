# Permanent Rule: Do Not Run flutter analyze

## Rule Description
- **NEVER** run `flutter analyze` under any circumstances.
- Do NOT invoke `flutter analyze` via terminal or subagents.
- Rely on direct code inspection, IDE lsp/diagnostics, and runtime verification without executing `flutter analyze`.
